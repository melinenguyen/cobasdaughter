.env
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.DS_Store
*.log
reports/latest.json
reports/latest.html

# Gmail OAuth — never commit these
scripts/client_secret.json
scripts/gmail_token.json
*.json

# ...but the archived trend reports must be committed: the daily workflow runs
# `git add reports/`, and the blanket *.json above was silently dropping every
# report JSON (only the HTML got committed) from 2026-05-27 onward.
!reports/trend_report_*.json
